const CreditHourRemaining = ({remainingHour}) => {
    return (
        <div>
            <h1 className="text-[#2F80ED] text-lg font-bold px-6 py-2">Credit Hour Remaining : {remainingHour} hr</h1>
        </div>
    );
};

export default CreditHourRemaining;